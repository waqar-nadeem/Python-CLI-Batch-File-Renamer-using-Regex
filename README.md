# Python-CLI-Batch-File-Renamer-using-Regex
This Python program automates bulk file renaming using regular expression patterns. It processes directories recursively, applies pattern matching and substitutions, and uses command-line arguments for flexible control. Demonstrates file handling, regex processing, OS operations, and scripting automation.
